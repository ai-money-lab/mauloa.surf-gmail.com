[
  {
    "id": "dryrun2_20260413_1648",
    "type": "python",
    "code": "import sys; sys.path.insert(0,'C:\\\\cits\\\\repo')\nfrom cits.japan.broker.kabu_api import KabuStationAPI\napi = KabuStationAPI()\npos = api.get_positions()\nprint('POSITIONS:', len(pos) if isinstance(pos,list) else pos)\norders = api.get_orders()\nprint('ORDERS:', len(orders) if isinstance(orders,list) else orders)\nprint('API_DRYRUN_OK')",
    "timeout": 20
  }
]
